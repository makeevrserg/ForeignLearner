<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="?attr/colorPrimaryDark"
    android:orientation="vertical">


    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="5sp"
        android:background="?attr/colorPrimary"
        android:padding="10sp">

        <TextView
            android:id="@+id/textView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Выберите словарь для импорта"
            android:textColor="?attr/textColor" />

        <Spinner
            android:id="@+id/spinner"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_alignParentEnd="true"
            android:layout_toRightOf="@id/textView"
            android:backgroundTint="?attr/colorAccent"
            android:textAlignment="textEnd"
            android:tooltipText="Выберите словарь для импорта" />
    </RelativeLayout>

    <Button
        android:id="@+id/buttonExportWords"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="5sp"
        android:background="?attr/colorPrimary"
        android:padding="10sp"
        android:text="Экспортировать словарь"
        android:textAlignment="textStart"
        android:textAllCaps="false"
        android:textColor="?attr/textColor" />

    <Button
        android:id="@+id/buttonTestDocument"
        android:layout_marginVertical="15sp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="5sp"
        android:background="?attr/colorPrimary"
        android:padding="10sp"
        android:text="Создать пример документа для импорта"
        android:textAlignment="textStart"
        android:textAllCaps="false"
        android:textColor="?attr/textColor" />
</LinearLayout>